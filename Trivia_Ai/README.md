# Running Mistral locally with Ollama and Node

This repo is a proof-of-concept for running Mistral models on your computer using Ollama, and interacting with it via a Node.js project.

How to run:

```
npm install
npm start
```